# JBTD — Job To Be Done (MVP scope)

> Полный список user stories для MVP ODIN. Группировка по областям продукта. Теги милестоунов — для понимания "к какому слою относится фича", не для дисциплины спринтов (см. JTBD-full-scope.md про связь milestones и спринтов).

> **Статус:** валидирован автором 2026-04-25. Все ранее открытые вопросы закрыты в пользу включения в MVP. Финальный список переедет в `<project>/docs/JTBD-full-scope.md` под управление Cowork.

---

## Легенда

- `[ ]` — не сделано
- `[x]` — сделано (реализовано в текущем прототипе)
- `[~]` — частично сделано / в процессе
- **Tag `[M-N]`** — отношение к milestone из JTBD-full-scope.md
- **`★`** — критично для MVP demo-сценария (Хугин-демо)
- **`◆`** — оригинальная story автора (9 штук)

---

## 1. Установка и первый запуск [M3, M9]

- [ ] Как пользователь я хочу установить ODIN одной командой / инсталлятором (.dmg для Mac / .exe для Windows / .AppImage для Linux), чтобы начать использовать без сложной настройки `[M9]`
- [ ] Как пользователь я хочу видеть приветственный onboarding-экран с кратким объяснением "что это и как использовать", чтобы понимать с чего начать `[M9]`
- [ ] Как пользователь я хочу указать путь к моему vault'у при первом запуске (или принять дефолтный), чтобы знать где лежат мои данные `[M3]`
- [ ] Как пользователь я хочу указать **имя/email для git config** при первом запуске, чтобы мои изменения подписывались правильно (используется и для `created_by` в anomaly) `[M3, M-SYNC]`

### Setup wizard — выбор источника каталога метрик

В процессе настройки перед первым запуском пользователь выбирает один из вариантов работы с каталогом:

- [ ] Как пользователь я хочу при первом запуске **развернуть дефолтный каталог из 236 метрик iGaming**, чтобы сразу иметь готовый каталог для исследования `[M2]`
- [ ] Как пользователь я хочу **развернуть дефолтный каталог и редактировать его** под свою компанию, чтобы стартовать с готового набора и адаптировать под свои нужды `[M2, M-EDIT]`
- [ ] Как пользователь я хочу **подключить vault к существующему git remote команды** (склонировать репо коллеги), чтобы сразу подхватить общую базу знаний компании `[M-SYNC]`
- [ ] Как пользователь я хочу **импортировать каталог из своей схемы** (из своих MD-файлов или другого формата), чтобы использовать ODIN с уже существующим списком метрик — *post-MVP, в setup wizard в MVP это заглушка "Coming soon"* `[post-MVP]`

---

## 2. Просмотр каталога метрик [M1]

- [x] ◆ Как пользователь я хочу посмотреть **все метрики из витрины в каталоге метрик**, чтобы видеть полный набор доступных метрик `[M1]`
- [x] ◆ Как пользователь я хочу посмотреть **все метрики из витрины в графе зависимостей**, чтобы понимать связи между метриками `[M1]`
- [x] ◆ Как пользователь я хочу посмотреть **любую метрику в детализированной карточке**, чтобы изучить её во всех аспектах (определение, формула, бенчмарки, ловушки, разрезы) `[M1]`
- [x] ◆ Как пользователь я хочу **отсортировать метрики** в каталоге или графе (по уровню, приоритету, категории, owner), чтобы быстро находить нужное `[M1]`
- [ ] Как пользователь я хочу **искать метрику по названию** через fuzzy search, чтобы быстро находить метрику не помня точного названия `[M1]`
- [ ] Как пользователь я хочу **фильтровать метрики** по уровню (L1/L2/L3), приоритету (Must/Should/Nice), категории, owner, источнику, чтобы сужать выборку `[M1]`
- [ ] Как пользователь я хочу чтобы **фильтры работали консистентно между всеми разделами** (Catalog / Graph / Saved), чтобы переключаясь между видами не терять контекст выборки `[M1]`
- [ ] Как пользователь я хочу **переключаться между светлой и тёмной темой**, чтобы выбрать комфортный для глаз режим `[M1]`
- [ ] Как пользователь я хочу **сравнить 2-3 метрики side-by-side**, чтобы видеть их различия (формулы, бенчмарки, разрезы) рядом `[M1]`

---

## 3. Редактирование смысла метрики [M-EDIT] ★

> Критично для ICP — аналитики не используют git/CLI. См. decisions-log.md ADR-011.

### Базовое редактирование

- [ ] ★ ◆ Как пользователь я хочу **внести изменения в описание метрики через карточку метрики посредством WYSIWYG-редактора**, чтобы править спеки без знания markdown/git `[M-EDIT]`
- [ ] ★ Как пользователь я хочу **редактировать frontmatter-поля как формочки** (dropdown для уровня, input для формулы и т.д.), чтобы менять структурированные данные без работы с YAML `[M-EDIT]`
- [ ] ★ Как пользователь я хочу видеть **индикатор статуса сохранения** ("Сохранено", "Сохраняется..."), чтобы быть уверенным что мои правки не потеряются `[M-EDIT]`
- [ ] Как пользователь я хочу **отменить изменения** до сохранения (Esc / Cancel button), чтобы не бояться экспериментировать в редакторе `[M-EDIT]`

### CRUD метрик в каталоге

- [ ] Как пользователь я хочу **добавить новую метрику** через UI (создать с нуля, заполнить поля, сохранить), чтобы расширять каталог своими метриками `[M-EDIT]`
- [ ] Как пользователь я хочу **удалить метрику** из каталога с подтверждением, чтобы убирать ненужные `[M-EDIT]`

### Производные метрики (derived metric с фильтром)

> Гибридный подход с двусторонней связью anomaly-заметок. См. **архитектурное примечание** ниже.

- [ ] Как пользователь я хочу **создать дубликат метрики с зашитым фильтром** (например, "GGR · filtered by GEO:US" из родительской GGR), чтобы мониторить срез метрики как отдельную единицу в каталоге `[M-EDIT]`
- [ ] Как пользователь я хочу видеть **производные метрики в каталоге как отдельные карточки** с пометкой "filtered: GEO=US" и ссылкой на родительскую, чтобы понимать происхождение `[M-EDIT]`
- [ ] Как пользователь я хочу **писать anomaly-заметки в производной метрике**, чтобы фиксировать инциденты на конкретном срезе (например, "просадка GGR в US 18 апреля") `[M-EDIT, M6]`
- [ ] Как пользователь я хочу чтобы **anomaly-заметки производной метрики отражались в родительской** (двусторонняя связь), чтобы при просмотре GGR видеть и общие инциденты, и инциденты её срезов — *см. архитектурное примечание* `[M-EDIT, M6]`
- [ ] Как пользователь я хочу **редактировать производную метрику независимо от родительской** (свой текст, свои ловушки, свои бенчмарки), чтобы не быть привязанным к шаблону родителя `[M-EDIT]`

> **Архитектурное примечание (для M-EDIT и M6):**
> Гибридный подход означает что производная метрика — это **самостоятельная сущность** в каталоге (свой `metric_id`, своя карточка, свой набор anomaly-заметок), но **технически связана** с родительской через ссылку и фильтр. Anomaly-заметка производной метрики **видна и в карточке родительской** (как "инцидент в срезе"), но не наоборот — общие инциденты родительской не дублируются в каждый срез.
>
> Это требует:
> - Поля в frontmatter spec.md производной: `parent_metric_id`, `filter` (например, `geo: us`)
> - Поля в frontmatter anomaly.md: помимо `metric_id` — опциональный список `also_visible_in: [parent_metric_id]`
> - В UI карточки родителя: секция "Anomaly от срезов" отдельно от собственных
>
> **Это open question по реализации, не блокер MVP.** Финализация формата — в M-EDIT.

---

## 4. История изменений и version control [M-EDIT] ★

> Под капотом git, в UX этого нет. См. ADR-011.

- [ ] ★ ◆ Как пользователь я хочу **просмотреть лог внесённых изменений через карточку метрики**, чтобы видеть кто что и когда менял `[M-EDIT]`
- [ ] ★ Как пользователь я хочу **видеть изменения в человекочитаемом diff** (не raw `+/-`, а "было: X, стало: Y"), чтобы понимать суть изменения `[M-EDIT]`
- [ ] ★ Как пользователь я хочу **вернуть метрику к предыдущей версии** одной кнопкой, чтобы откатить ошибочные изменения `[M-EDIT]`
- [ ] Как пользователь я хочу видеть **автоматически генерируемые сообщения коммитов** ("Updated GGR Margin formula", "Changed thresholds for FTD Count"), чтобы понимать что изменилось без копания в diff `[M-EDIT]`

---

## 5. Anomaly-заметки (институциональная память) [M6, M-EDIT]

- [ ] Как пользователь я хочу **видеть связанные с метрикой anomaly-заметки** в её карточке (история инцидентов с этой метрикой), чтобы знать что уже случалось с метрикой и как это решалось `[M6]`
- [ ] Как пользователь я хочу **прочитать anomaly-заметку** в человекочитаемом виде (не raw MD), чтобы быстро понять что произошло, что выяснилось, какая ловушка сработала, как починили `[M6]`
- [ ] Как пользователь я хочу **создать новую anomaly-заметку через UI** для метрики, чтобы зафиксировать новый инцидент в институциональной памяти `[M-EDIT]`
- [ ] Как пользователь я хочу **связать anomaly-заметку с конкретной ловушкой из спеки** (wiki-link), чтобы видеть какой риск из спеки реализовался `[M6, M-EDIT]`
- [ ] Как пользователь я хочу видеть **timeline всех anomaly на уровне vault** (не только конкретной метрики), чтобы понимать общий паттерн инцидентов в системе метрик `[M-EDIT, post-MVP late]`

---

## 6. Сохранённые метрики и отчёты [M1]

- [x] ◆ Как пользователь я хочу **сохранить одну или несколько метрик в раздел "сохранённое"** для мониторинга или формирования отчёта по ним, чтобы держать важные метрики под рукой `[M1]`
- [x] ◆ Как пользователь я хочу **выгрузить описание метрики в .md формате** через карточку, чтобы использовать как контекст для LLM или поделиться `[M1]`
- [ ] Как пользователь я хочу **выгрузить отчёт по нескольким сохранённым метрикам** (digest), чтобы получить структурированный документ для команды `[M1]`

---

## 7. Данные и значения метрик [M4, M5]

### Просмотр данных

- [ ] Как пользователь я хочу видеть **текущее значение метрики** в её карточке, чтобы понимать актуальное состояние `[M4]`
- [ ] Как пользователь я хочу видеть **тренд (sparkline) метрики** за последний период, чтобы быстро оценить динамику `[M4]`
- [ ] Как пользователь я хочу видеть **изменение метрики WoW и MoM** в карточке, чтобы понимать краткосрочную и среднесрочную динамику `[M4]`
- [ ] Как пользователь я хочу видеть **историю значений метрики на полноценном графике** (не sparkline), чтобы изучить динамику в деталях `[M4]`
- [ ] Как пользователь я хочу видеть **значение метрики в точке времени при наведении на график** (hover-tooltip), чтобы получать конкретные числа без отдельных кликов `[M4]`
- [ ] Как пользователь я хочу **переключать график между разрезами** (overall / по гео / по продукту), чтобы изучать метрику в разных проекциях `[M4]`
- [ ] Как пользователь я хочу **сравнить динамику метрики по нескольким значениям одного разреза side-by-side** (например, "GGR по US vs UK vs DE на одном графике"), чтобы видеть различия `[M4]`
- [ ] Как пользователь я хочу **скачать отчёт по сравнению нескольких разрезов** (например, CSV с GGR по US/UK/DE за период), чтобы использовать в дальнейшем анализе `[M4]`
- [ ] Как пользователь я хочу видеть на графике **пороговые линии** (red/yellow), чтобы наглядно понимать когда метрика входит в зону тревоги `[M4]`
- [ ] Как пользователь я хочу **выбрать кастомный date range** для графика, чтобы исследовать конкретный период `[M1, Q-UX.3]`

### Watchlist и алерты

- [ ] Как пользователь я хочу видеть **dashboard с метриками, требующими внимания** (red/yellow alerts по порогам), чтобы знать на что смотреть в первую очередь `[M4]`
- [ ] Как пользователь я хочу видеть **счётчики метрик по статусам** (X red / Y yellow / Z ok), чтобы быстро оценить общее здоровье набора метрик `[M4]`
- [ ] Как пользователь я хочу **получать push-уведомления о выходе метрики в red/yellow** на email (опционально — Slack), чтобы реагировать без необходимости постоянно открывать ODIN `[M-EDIT, post-MVP late]`

### Sync с витриной

- [ ] Как пользователь я хочу **указать источник данных** (локальный Parquet-файл / watch-folder / S3-bucket / HTTP endpoint), чтобы ODIN тянул витрину автоматически `[M5]`
- [ ] Как пользователь я хочу видеть **результат валидации витрины** при первом импорте (какие metric_id найдены, какие отсутствуют, какие неизвестные), чтобы понимать что ODIN видит и не видит `[M5]`
- [ ] Как пользователь я хочу видеть **timestamp последнего sync** ("Обновлено 25 апреля, 09:42"), чтобы понимать насколько данные свежие `[M5]`
- [ ] Как пользователь я хочу **вручную инициировать sync** через кнопку, чтобы не ждать периодической проверки когда нужно срочно обновить `[M5]`
- [ ] Как пользователь я хочу **задать частоту автоматического обновления** (раз в час / день / по cron), чтобы не дёргать sync вручную `[M5]`
- [ ] Как пользователь я хочу **получить документацию по контракту витрины** для передачи моему data-инженеру (требования к схеме Parquet-файла), чтобы он построил витрину правильно `[M5]` — см. munin-sync-contract.md

### Экспорт значений

- [x] ◆ Как пользователь я хочу **выгрузить статистику по метрике через экспорт в csv**, чтобы использовать в других инструментах `[M1, M4]`
- [ ] Как пользователь я хочу **bulk-экспорт нескольких метрик** в едином формате (long-format CSV или ZIP), чтобы выгрузить набор для отчёта `[M1, Q-UX.2]`

---

## 7b. Vault sync — shared semantic layer для команды [M-SYNC]

> Поэтапная реализация. См. decisions-log.md ADR-013. В MVP — single-user + опциональный git remote, без conflict UI и RBAC.

### Подключение и базовый sync

- [ ] Как пользователь я хочу **подключить мой vault к git remote** (GitHub / GitLab / Gitea / корпоративный bare git), чтобы синхронизировать метрики с командой `[M-SYNC]`
- [ ] Как пользователь я хочу **указать credentials к git remote** (SSH-ключ или Personal Access Token) через credentials store, чтобы push/pull работали без CLI `[M-SYNC, ADR-012]`
- [ ] Как пользователь я хочу **вручную инициировать push** через кнопку в Settings → Vault sync, чтобы отправить свои изменения коллегам `[M-SYNC]`
- [ ] Как пользователь я хочу **вручную инициировать pull** через кнопку, чтобы получить изменения коллег `[M-SYNC]`

### Автоматизация

- [ ] Как пользователь я хочу **auto-pull при старте ODIN** (опция, default off), чтобы при запуске видеть свежую версию vault'а `[M-SYNC]`
- [ ] Как пользователь я хочу **auto-push при save метрики** (опция, default off), чтобы изменения сразу попадали в shared vault — *по умолчанию off, чтобы избежать спама коммитов* `[M-SYNC]`
- [ ] Как пользователь я хочу видеть **timestamp последнего успешного pull/push** в Settings, чтобы понимать когда синхронизировался `[M-SYNC]`

### Понятная индикация конфликтов

- [ ] Как пользователь я хочу **видеть понятное сообщение при git-конфликте** ("Анна изменила GGR в 14:32, ваша версия не может быть запушена пока конфликт не разрешён"), чтобы понимать что произошло `[M-SYNC]`
- [ ] [post-MVP] Как пользователь я хочу **разрешать конфликты внутри ODIN** (не через git CLI), чтобы не выходить из приложения — *Этап 2 ADR-013, post-MVP* `[post-MVP]`

### Что НЕ в MVP (зафиксировано в ADR-013)

- ❌ Auto-write Хугина в shared vault — в MVP только "Save as anomaly note" по кнопке
- ❌ UI разрешения конфликтов внутри ODIN — fallback на git CLI с понятным сообщением
- ❌ Real-time sync (как Obsidian Sync) — только manual + auto-pull при старте
- ❌ RBAC (видимость метрик по ролям) — Этап 3 (post-MVP)
- ❌ Multi-user awareness ("Анна редактирует GGR прямо сейчас") — post-MVP

---

## 8. Хугин: LLM-помощник [M7, M8] ★

### Подключение модели

- [ ] ★ Как пользователь я хочу **подключить локальную модель** (Ollama / LM Studio / OpenAI-compatible endpoint) или Claude Desktop через MCP, чтобы выбрать LLM по своим compliance-требованиям `[M8]`
- [ ] Как пользователь я хочу **переключаться между разными моделями** в UI, чтобы сравнивать качество или использовать разные для разных задач `[M8]`

### Диалог с агентом

- [ ] ★ Как пользователь я хочу **спросить агента "почему просела метрика X"** и получить ответ с гипотезой, опираясь на спеку, исторические значения и прошлые аномалии `[M7, M8]`
- [ ] Как пользователь я хочу **попросить агента показать график метрики по разрезу** ("GGR по гео", "Retention D7 по сегменту"), чтобы получить визуализацию через диалог `[M7]`
- [ ] Как пользователь я хочу **продолжать диалог с агентом** в карточке метрики (chat history), чтобы углубляться в анализ без потери контекста `[M7, M8]`
- [ ] Как пользователь я хочу видеть **что именно агент прочитал** (какие спеки, какие данные, какие аномалии) при формировании ответа, чтобы доверять или проверять его выводы `[M7]`

### Визуализация в ответе агента

- [ ] Как пользователь я хочу чтобы **агент мог вернуть график как часть ответа** (не только текст), чтобы получать визуальные ответы там, где они уместнее текста `[M7, post-MVP late]`
- [ ] Как пользователь я хочу чтобы **агент мог вернуть таблицу как часть ответа**, чтобы видеть структурированные данные без отдельных запросов `[M7, post-MVP late]`

> **Техническое примечание:** реализуется через MCP-инструмент `render_chart(spec)` / `render_table(data)` — агент возвращает спецификацию визуализации, ODIN рендерит. Архитектурное решение откладывается до M7.

### Экспорт результатов

- [ ] Как пользователь я хочу **сохранить интересный диалог с агентом как anomaly-заметку**, чтобы зафиксировать инсайт в институциональной памяти `[M-EDIT, M7, post-MVP late]`
- [ ] Как пользователь я хочу **экспортировать ответ агента в файл** — текст в .md, график/таблицу в .csv — чтобы использовать в дальнейшей работе вне ODIN `[M7, post-MVP late]`

---

## 9. UX-долги текущего прототипа [M1]

> См. open-questions.md Q-UX.1 — Q-UX.4.

- [ ] Как пользователь я хочу видеть **консистентный preview метрики** в Graph и Catalog режимах (один компонент, одни данные), чтобы не путаться при переключении `[M1, Q-UX.1]` — **в Sprint 2**
- [ ] Как пользователь я хочу **bulk export разных метрик с разными размерностями** в едином long-format CSV (без потери данных), чтобы выгружать смешанные наборы `[M1, Q-UX.2]`
- [x] Как пользователь я хочу чтобы **мелкие интерфейсные баги** (TBD: список) были устранены, чтобы прототип работал стабильно `[M1, Q-UX.4]` — *Sprint 1: контраст заголовков карточек + drawer; полный список собран в Sprint 2 prompt*
- [ ] Как пользователь я хочу **унифицированные фильтры** между Catalog и Graph view (один компонент, одинаковый порядок: Alerts → Level → Priority → Owner → Freq → Category) `[M1, Sprint 2]`
- [ ] Как пользователь я хочу видеть **счётчик "X из Y метрик"** в control-bar, чтобы понимать сколько отфильтровано из общего числа `[M1, Sprint 2]`
- [ ] Как пользователь я хочу **сворачивать секции L1/L2/L3** на Graph и Catalog по клику на подпись секции, чтобы фокусироваться на нужном уровне без полной фильтрации `[M1, Sprint 2]`
- [ ] Как пользователь я хочу видеть **alerts-счётчик "X red · Y yellow · Z ok"** в control-bar Catalog (как сейчас на Home), чтобы быстро оценить здоровье метрик `[M1, Sprint 2]`
- [ ] Как пользователь я хочу чтобы **Graph view имел такой же control-bar** как Catalog (с Filters, zoom, счётчиками), а не разрозненные элементы по углам `[M1, Sprint 2]`
- [ ] Как пользователь я хочу чтобы **подписи всех нод графа** были постоянно видны (не только L1, не только на hover) `[M1, Sprint 2]`
- [ ] Как пользователь я хочу чтобы **тултип на ноде графа** показывал состояние метрики (Red/Yel/OK + значение), а не нечитаемый чёрный текст `[M1, Sprint 2]`
- [ ] Как пользователь я хочу чтобы **семантика уровней L1/L2/L3** была правильной (L1 Стратегические для C-level, L2 Операционные для команд, L3 Диагностические для deep-dive), а не как в прототипе Claude Design `[M1, Sprint 2]`

---

## 9b. Tech debt прототипа (M1-cleanup, Sprint 2)

> Tech debt из Sprint 1 sprint-report. Часть выпиливается в Sprint 2, часть откладывается.

- [ ] **Выпил tweaks-panel** — рудимент Claude Design (интерактивные «твиксы» для дизайн-итераций), убрать целиком: `tweaks-panel.jsx`, `tweaks-ui.jsx`, и все `tw`-пропсы из `app.jsx` props chain `[Sprint 2]`
- [ ] **Унифицировать дубликаты функций** — `cx`, `gSparkPath`/`gFakeValue`/`gFakeDelta` живут параллельно в `graph-view.jsx` и `app.jsx` (артефакт script-tag режима). Вынести в `lib/util.js` `[Sprint 2]`
- [ ] **Загрузить 236 метрик из seed JSON** вместо захардкоженных 47 в `data.js` `[Sprint 2]` — см. `default-vault/_seed/igaming_metrics.json`
- [ ] **Bundled-fonts вместо Google Fonts CDN** — `@import url('https://fonts.googleapis.com/...')` в `styles.css` нарушит ADR-008 (local-first) при упаковке в Electron. Фикс до M3 `[pre-M3]`
- [ ] **Saved/watchlist persistence** — заменить browser `localStorage` на file-based store или config (для Electron). Отложено `[M3 или отдельный спринт]`
- [ ] **Graph scaling на 236 нодах** — текущий layout перегружен, подписи перекрываются. Sprint 2 fix-cycle разнесёт ноды и (опционально) добавит auto-hide labels at low zoom — это **temporary mitigation**. Полное решение в Sprint 3:
  - **Filter-driven graph** (рекомендация Cowork) — по дефолту L2/L3 свёрнуты (после Sprint 2 fix), показывается только L1 ~58 нод. Если пользователь хочет глубже — фильтрует по L2/L3 и видит только релевантные. Граф = investigation tool, не overview map.
  - Альтернативы (не рекомендую): force-directed layout (d3-force), clustering, virtualization. Дороже в реализации, не решают product-проблему.
  - **Сворачивание отдельных нод** — обсуждено и отклонено: создаёт state hell, конфликт с pin-механикой. Текущий collapse на уровне секций L1/L2/L3 — правильная гранулярность.
- [ ] **Catalog scroll visibility** — после загрузки 236 метрик скрытый scrollbar делает прокрутку «слепой» (непонятен прогресс). Варианты: видимый кастомный scrollbar (5 строк CSS, рекомендую) / sticky header с прогресс-баром / virtualized list (overkill для 236) `[Sprint 3]`

---

## 10. Демо и публикация [M9]

> Не user stories для пользователя продукта, а артефакты выпуска.

- [ ] ★ Записать **90-секундное demo-видео** с прохождением сценария 3 (Хугин объясняет аномалию) `[M9]`
- [ ] Подготовить **README** проекта с инструкцией установки `[M9]`
- [ ] Подготовить **about page** в самом ODIN с атрибуцией, концепцией и контактами `[M9]`
- [ ] Опубликовать **GitHub Releases** с .dmg / .exe / .AppImage `[M9]`
- [ ] Опубликовать в **LinkedIn / Telegram / GitHub** (по решению автора) `[M9]`

---

## Cross-cutting (без привязки к milestone)

### Credentials store — для всех секретов (ADR-012)

- [ ] Как пользователь я хочу **управлять credentials через Settings → Credentials**, чтобы видеть какие учётки настроены и обновлять их `[M-SYNC, M5, M8]`
- [ ] Как пользователь я хочу видеть **только статус "Set / Not set"** для каждого секрета (а не сам секрет), чтобы случайно не показать ключ при скриншоте `[M-SYNC, M5, M8]`
- [ ] Как пользователь я хочу что credentials хранились в **OS Keychain / Credential Manager**, а не в файлах vault'а, чтобы они не попали в git remote по ошибке `[M-SYNC, M5, M8]`

### Документация и setup

- [x] Документация: контракт витрины для data-инженеров — `docs/context/munin-sync-contract.md`
- [x] Документация: CLAUDE.md в корне проекта (для Claude Code)
- [x] Документация: ADR живут в `docs/context/decisions-log.md` (один файл, без отдельной папки)
- [ ] Документация: руководство по подключению git remote (как создать репо, как сгенерировать SSH-ключ, как добавить в ODIN)
- [ ] Setup: инструкции по `~/.claude/settings.json` для Claude Code

---

## Снято с повестки (was on list, not anymore)

- ❌ ~~Ссылки "edit on GitHub" / "open in Obsidian" из карточки~~ — редактирование через встроенный WYSIWYG, см. ADR-011
- ❌ ~~Hosted version / public web demo~~ — local-first, см. ADR-008
- ❌ ~~Multi-user collaboration~~ — single-user, post-MVP

---

## Post-MVP идеи (зафиксированы, не обязательство)

Эти фичи озвучены автором как "хочется", но **не для первого MVP**. Зафиксированы здесь чтобы не потерять идею.

### Architecture-relevant (могут потребовать backwards-incompatible изменений если думать о них поздно)

- **RBAC — видимость метрик по ролям.** Кейс: казино не светят финансовые метрики перед младшими аналитиками. Если когда-то будет multi-user, RBAC должен быть встроен с самого начала. **Архитектурный hint:** не закладывать решений сейчас, которые сделают невозможным introduction of роли позже (например, при сохранении anomaly-заметки запоминать `created_by` уже сейчас, даже если в MVP это всегда single user).

### UX-фичи

- **Конструктор дашборда** — pro-режим, отдельная фича. Пользователь сам собирает дашборд из карточек метрик. Большой кусок работы, отдельная разработка.
- **Импорт каталога из своей схемы** при первом запуске (option 3 в setup wizard) — в MVP это заглушка "Coming soon".

### Расширения интеграций

- **HTTP endpoint, SFTP** как дополнительные sync-методы (S3-bucket уже в MVP по решению).
- **Cloud git remote** — push/pull, разрешение конфликтов, multi-user через GitHub/GitLab/Gitea.

---

## История валидации

- **2026-04-25 (initial draft):** Claude расширил оригинальные 9 stories автора до 60. 5 пунктов помечены как `[?]` (требующие решения).
- **2026-04-25 (валидация):** автор закрыл все `[?]` в пользу включения в MVP, добавил 12 новых stories и расширений (setup wizard, консистентность фильтров, дубликат метрики с двусторонней связью, hover-tooltip, сравнение разрезов, push-алерты, S3-bucket, частота sync, конструктор дашборда post-MVP, RBAC post-MVP, экспорт ответа агента, видимость anomaly производных в родителе).
- **Текущее состояние:** этот документ. Подготовлен к переносу в `<project>/docs/JTBD-full-scope.md` под управление Cowork.

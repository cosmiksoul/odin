# Test Cases — Sprint 2 (Cleanup + Real Data + UX Consistency)

**Дата:** 2026-04-26
**Подготовил:** Cowork
**Тестирует:** N13Z
**Что тестируем:** результат Sprint 2 — выпил tweaks-panel, 236 метрик из JSON, общий ControlBar/Filters/MetricPreview, правильная семантика L1/L2/L3, постоянные подписи нод, читаемый тултип.

> **Состояние:** Pass 1 закоммичен (`280baf7`). Pass 2 готов, **ещё не закоммичен** — ждёт твоего ревью + QA.
>
> Эти тест-кейсы **дополняют** «How to test» из `sprint-report-2.md`. Code сделал свой чек-лист (хорошо знает что менял) — я взял за baseline и добавил: diagnostic для известных Concerns + регрессию Pass 1 + проверку известных проблем (zoom, scrollbar) для подтверждения.
>
> Если что-то падает — заполни «Факт», помечу в Decision Log.

---

## Подготовка

```bash
cd /c/Users/cosmi/Projects/odin
npm run dev
```

Открыть `http://localhost:5173/`. DevTools (F12) — открыты, console видна.

---

## TC-1: Pass 1 регрессия — 236 метрик загрузились

**Цель:** убедиться что после Pass 2 Pass 1 не сломан.

**Шаги:**
1. CATALOG — счётчик в TopBar и в ControlBar показывает `236 / 236`
2. Прокрутить — карточек видно реально много (не 47)
3. Любая карточка имеет реальные данные (название, категория, formula, traps)

**Ожидаемое:** 236 метрик загружены и рендерятся.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-2: Pass 1 регрессия — Tweaks panel отсутствует

**Цель:** убедиться что выпил tweaks-panel не отвалился назад.

**Шаги:**
1. Просмотреть углы экрана (особенно правый нижний — там был tweaks)
2. Никакой свёрнутой иконки / выпадающей панели «Tweaks»

**Ожидаемое:** ничего нет.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-3: ControlBar consistency между Catalog и Graph

**Цель:** Code сделал общий компонент — проверить что выглядит одинаково.

**Шаги:**
1. CATALOG — посмотреть на полоску под TopBar: `[☰ FILTERS] [N / 236 metrics] [● R · ● Y · ● OK] ... [SIZE S/M/L] [Grid/List]`
2. Переключиться на GRAPH — **та же полоска**, на тех же позициях, только справа `[− %% + fit]` вместо size+layout
3. Зацепить взглядом высоту, padding, цвет фона, шрифты — должны быть **визуально идентичны**

**Ожидаемое:** ControlBar один компонент, выглядит одинаково на обоих view (различие только в правой view-specific секции).

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-4: Счётчик динамически реагирует на фильтрацию

**Цель:** проверить counter X / Y и alerts-counter.

**Шаги:**
1. На CATALOG открыть Filters (`☰ FILTERS`)
2. В секции PRIORITY выбрать `Must`
3. Закрыть drawer
4. Проверить ControlBar: счётчик `X / 236` показывает только Must-метрики (X < 236)
5. Alerts-counter `● R · ● Y · ● OK` — числа тоже изменились (среди отфильтрованных)
6. Снять фильтр → счётчики возвращаются к 236

**Ожидаемое:** оба счётчика обновляются live.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-5: Фильтры унифицированы

**Цель:** один drawer для обоих view, правильный порядок секций.

**Шаги:**
1. На CATALOG открыть `☰ FILTERS`
2. Проверить порядок секций: **ALERTS → LEVEL → PRIORITY → OWNER → FREQ → CATEGORY**
3. Выбрать в ALERTS → `red`
4. Закрыть drawer
5. Переключиться на GRAPH
6. Открыть `☰ FILTERS` — **тот же drawer**, **тот же state** (фильтр red уже применён)

**Ожидаемое:** фильтры shared between routes, drawer один.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-6 (diagnostic для Concern C1): Multi-pin на Graph отсутствует

**Цель:** подтвердить или опровергнуть Concern C1 из code-review.

**Контекст:** до Sprint 2 на Graph был side-panel со стэком pinned-карточек (можно было закрепить N метрик). Code в Pass 2 заменил это на single Drawer overlay (как в Catalog) — multi-pin удалён. Я зафлагал это как Concern C1, ждал твою оценку в QA.

**Шаги:**
1. На GRAPH кликнуть на любую ноду
2. Что происходит? **Ожидаемое поведение Code'a (после Pass 2):** открывается Drawer как в Catalog. Никакой side-panel со стэком.
3. Кликнуть на вторую ноду
4. **Что происходит?** В Pass 2: Drawer переключается на новую метрику. Старая никуда не «закреплена».

**Вопрос (твоё решение):**
- ☐ Single Drawer мне ОК — multi-pin не нужен (consistency лучше)
- ☐ Multi-pin нужен — возвращаем в fix-cycle

**Факт:**

**Статус:** ☐ Single OK / ☐ Need multi-pin

---

## TC-7: Drawer / pinned карточка — единый MetricPreview

**Цель:** проверить что MetricPreview одинаково выглядит в Catalog и Graph.

**Шаги:**
1. На CATALOG кликнуть на карточку GGR (или любую) → выезжает Drawer справа
2. Запомнить визуально: header, breadcrumb, секции (Что/Формула/Бенчмарки/Пороги/Ловушки/Как чинить/Связи), исторический график
3. Закрыть Drawer
4. Перейти на GRAPH, найти ту же ноду GGR в L1, кликнуть
5. Открывается Drawer с **тем же layout** — содержимое идентично

**Ожидаемое:** layout 1-в-1, контент идентичен. Возможные отличия — breadcrumb (в Pass 2 убрали `Catalog /` префикс, теперь `cat / name` в обоих случаях).

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-8: Семантика L1/L2/L3 правильная

**Цель:** проверить что Code сделал правильные подписи (не Claude Design'овские).

**Шаги:**
1. На GRAPH посмотреть подписи 3 колонок:
   - **L1: СТРАТЕГИЧЕСКИЕ** — C-level · daily review · падение = проблема бизнеса
   - **L2: ОПЕРАЦИОННЫЕ** — Product · CRM · Marketing · ищут причину изменений в L1
   - **L3: ДИАГНОСТИЧЕСКИЕ** — Deep-dive под аномалию из L2
2. Цвет подписей **читаемый** на тёмной теме (заметно ярче чем было)
3. На CATALOG то же — есть три секции с такими же подписями (header'ы перебивок)

**Ожидаемое:** правильные определения, контраст ОК, секции в каталоге есть.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-9: Collapsible L1/L2/L3 + persist

**Цель:** проверить collapsible на обоих view + сохранение в localStorage.

**Шаги:**
1. На CATALOG — кликнуть на header `▾ L1 СТРАТЕГИЧЕСКИЕ` → секция сворачивается, иконка меняется на `▸`, карточки уровня скрываются
2. Перезагрузить страницу (F5) — секция **остаётся свёрнутой** (localStorage)
3. Развернуть обратно
4. На GRAPH то же — кликнуть на подпись колонки `L1: СТРАТЕГИЧЕСКИЕ` сверху → ноды и edges уровня скрываются, иконка меняется
5. Reload — состояние сохраняется

**Ожидаемое:** работает на обоих view, persist работает.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-10: Подписи нод графа постоянные + читаемый тултип

**Цель:** проверить пункты E1+E2 из Sprint 2.

**Шаги:**
1. На GRAPH без hover — **все ноды** (L1, L2, L3) имеют видимые подписи (не только L1)
2. L2/L3 — мельче, но читаемо
3. Hover на любую ноду → tooltip:
   - Status badge (RED/YEL/OK) с цветом
   - Текущее значение (типа `45.32%`)
   - WoW delta (▲/▼ с up/down цветом)
   - Название метрики, level · category
   - hint `click to open`
4. Tooltip **читаемый** (не чёрный текст на чёрном)

**Ожидаемое:** подписи везде, tooltip контентный и читаемый.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-11 (известная проблема — подтверждение): Zoom масштабирует ВСЁ

**Цель:** подтвердить зафлаганную ранее проблему (для fix-cycle).

**Шаги:**
1. На GRAPH нажать `+` несколько раз (или прокрутить колесом мыши zoom in)
2. Проверить: **что именно увеличилось?**
   - Ожидание (как у «больших мальчиков»): только ноды + edges + лейблы
   - Сейчас (по твоим скринам): увеличилось всё, включая ControlBar, кнопки fit/+/-, подписи L1/L2/L3 с фоном

**Ожидаемое:** проблема воспроизводится — zoom затрагивает UI overlay (это bug).

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL (FAIL = bug подтверждён)

---

## TC-12 (известная проблема — подтверждение): Catalog scroll без индикатора

**Цель:** подтвердить вторую известную проблему (для fix-cycle / Sprint 3).

**Шаги:**
1. На CATALOG прокрутить до конца списка метрик (или попробовать)
2. Видно ли где ты сейчас в списке?
3. Виден ли scrollbar?

**Ожидаемое:** scrollbar скрыт, прогресс непонятен (это известная проблема, в Sprint 2 не fix'или).

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL (FAIL = проблема воспроизводится — ОК для Sprint 3)

---

## TC-13: Темы DARK ↔ LIGHT работают

**Цель:** регрессия по Sprint 1.

**Шаги:**
1. В TopBar нажать `LIGHT`
2. Все компоненты (TopBar, ControlBar, FiltersDrawer, Drawer/MetricPreview, Catalog cards, Graph) — корректно переключились
3. Никаких чёрных текстов на чёрном или белого на белом
4. Вернуться `DARK`

**Ожидаемое:** обе темы работают, контраст везде ОК.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-14: DevTools console — нет красных Error

**Цель:** проверить что нет рантайм-ошибок (ловушка из Sprint 1 fix-cycle).

**Шаги:**
1. F12 → Console
2. F5 (перезагрузить страницу)
3. Просмотреть console output

**Ожидаемое:**
- Жёлтые warnings допустимы (deprecated API, key prop, missing keys — унаследованный tech debt)
- Красных Error быть не должно
- Особенно — никакой `XYZ is not defined` (как был React.useState в Sprint 1)

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-15: Производительность Graph при 236 нодах

**Цель:** Code в Notes написал «лагов не было визуально, но не замерял точно». Проверить эмпирично.

**Шаги:**
1. На GRAPH с включёнными всеми L1+L2+L3 (236 нод видны)
2. Попробовать pan (drag canvas)
3. Попробовать zoom in/out
4. Попробовать hover по нодам последовательно
5. Лагов не должно быть — отзывчивость нормальная

**Ожидаемое:** плавная работа.

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## TC-16 (опционально): Production-сборка

**Цель:** проверить что prod-bundle тоже работает (а не только dev).

**Шаги:**
1. Stop `npm run dev`
2. `npm run build`
3. `npm run preview`
4. Открыть URL preview, бегло прогнать TC-3, TC-7, TC-9, TC-10

**Ожидаемое:** prod-сборка визуально идентична dev. Bundle warning «>500KB» допустим (Code зафлагал).

**Факт:**

**Статус:** ☐ PASS / ☐ FAIL

---

## Decision Log по итогам QA

> Заполни после прохождения. Я подхвачу.

| TC | Status | Комментарий |
|---|---|---|
| TC-1 (236 метрик) | | |
| TC-2 (нет tweaks) | | |
| TC-3 (ControlBar consistent) | | |
| TC-4 (counter live) | | |
| TC-5 (filters унифицированы) | | |
| **TC-6 (multi-pin)** | | **single OK / need multi-pin** |
| TC-7 (Drawer == pinned) | | |
| TC-8 (L1/L2/L3 семантика) | | |
| TC-9 (collapsible + persist) | | |
| TC-10 (подписи + tooltip) | | |
| **TC-11 (zoom UI bug)** | | **подтвержд. / нет** |
| **TC-12 (scroll Catalog)** | | **подтвержд. / нет** |
| TC-13 (themes) | | |
| TC-14 (no Error) | | |
| TC-15 (perf Graph) | | |
| TC-16 (prod build) | | |

**Общий вердикт QA:**
- ☐ Sprint 2 closed (Pass 2 готов к коммиту, fix-cycle не нужен)
- ☐ Sprint 2 нужен fix-cycle — список failed TC и что чинить:
